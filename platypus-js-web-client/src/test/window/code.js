/* global expect */
/* global NaN */

describe('Window Api', function () {
    it('Caption.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Tools.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Events.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Selector.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Modal.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Undecorated.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Undecorated.Markup', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('LocationByPlatform.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Shown.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('Active.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('FormKey.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('DesktopPane.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane'
        ], function (
                Logger,
                WindowPane) {
            
            done();
        });
    });
    it('DesktopPane.LocationByPlatform.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane',
            'forms/containers/desktop-pane'
        ], function (
                Logger,
                WindowPane,
                DesktopPane) {
            
            done();
        });
    });
    it('DesktopPane.Shown.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane',
            'forms/containers/desktop-pane'
        ], function (
                Logger,
                WindowPane,
                DesktopPane) {
            
            done();
        });
    });
    it('DesktopPane.Active.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane',
            'forms/containers/desktop-pane'
        ], function (
                Logger,
                WindowPane,
                DesktopPane) {
            
            done();
        });
    });
    it('DesktopPane.FormKey.Structure', function (done) {
        require([
            'logger',
            'forms/window/window-pane',
            'forms/containers/desktop-pane'
        ], function (
                Logger,
                WindowPane,
                DesktopPane) {
            
            done();
        });
    });
});
