allprojects{
    apply plugin: 'java'

    repositories{
        jcenter()
    }

    dependencies{
        testCompile 'junit:junit:4.11'
    }
}

subprojects{
    apply plugin: 'maven'

    repositories{
        mavenLocal()
    }
}