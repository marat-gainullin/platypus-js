/**
 * 
 * @author mg
 * @name Alerts
 */

function Alerts() {

    var self = this;

    P.Logger.info("arguments: " + arguments);
    P.Logger.info("arguments.length: " + arguments.length);
    P.Logger.info("arguments[0]: " + arguments[0]);
    P.Logger.info("arguments[1]: " + arguments[1]);
}