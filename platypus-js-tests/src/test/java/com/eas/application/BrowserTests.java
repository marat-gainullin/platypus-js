package com.eas.application;

import org.junit.Before;
import org.junit.Ignore;
import org.junit.Test;
//import org.openqa.selenium.By;
//import org.openqa.selenium.WebDriver;
//import org.openqa.selenium.chrome.ChromeDriver;

/**
 *
 * @author mg
 */
public class BrowserTests {
    
    //private WebDriver browser;
    
    public void init(){
        //browser = new ChromeDriver();
    }

    public void appendTest() {
        /*
        browser.get("http://localhost:8080/p-tests/");
        browser.findElement(By.id("#username")).sendKeys("testuser2");
        browser.findElement(By.id("#password")).sendKeys("test");
        browser.findElement(By.id("#submit")).submit();
        */
    }
}
